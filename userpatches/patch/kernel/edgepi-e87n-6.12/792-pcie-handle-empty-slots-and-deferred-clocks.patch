--- a/drivers/pci/controller/pcie-mediatek-gen3.c
+++ b/drivers/pci/controller/pcie-mediatek-gen3.c
@@ -451,6 +451,12 @@ static int mtk_pcie_startup_port(struct
 		ltssm_index = PCIE_LTSSM_STATE(val);
 		ltssm_state = ltssm_index >= ARRAY_SIZE(ltssm_str) ?
 			      "Unknown state" : ltssm_str[ltssm_index];
+		if (device_property_read_bool(pcie->dev,
+					      "mediatek,empty-slot-ok")) {
+			dev_info(pcie->dev, "PCIe slot has no endpoint\n");
+			return -ENODEV;
+		}
+
 		dev_err(pcie->dev,
 			"PCIe link down, current LTSSM state: %s (%#x)\n",
 			ltssm_state, ltssm_index);
@@ -873,8 +879,8 @@ static int mtk_pcie_parse_port(struct mt
 
 	pcie->num_clks = devm_clk_bulk_get_all(dev, &pcie->clks);
 	if (pcie->num_clks < 0) {
-		dev_err(dev, "failed to get clocks\n");
-		return pcie->num_clks;
+		return dev_err_probe(dev, pcie->num_clks,
+				     "failed to get clocks\n");
 	}
 
 	ret = of_property_read_u32(dev->of_node, "wifi-reset-msleep",
